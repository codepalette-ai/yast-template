{
  "name": "turbo-template",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "pnpm run db:init && turbo run dev",
    "lint": "ultracite lint ./apps",
    "format": "ultracite format ./apps",
    "test": "turbo run test",
    "analyze": "turbo run analyze",
    "install:web": "cd apps/web && pnpm install",
    "db:init": "cd packages/database && npx prisma format && npx prisma generate --no-engine",
    "db:migrate": "cd packages/database && npx prisma migrate dev",
    "check-types": "turbo run check-types",
    "bump-deps": "npx npm-check-updates --deep -u && pnpm install",
    "bump-ui": "npx shadcn@latest add --all --overwrite -c packages/design-system && pnpm run format"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.5",
    "ultracite": "5.0.25",
    "turbo": "^2.5.4",
    "typescript": "5.8.3"
  },
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=22"
  }
}
